package urender.api;

/**
 * Framebuffer clear mode mask.
 */
public enum UClearMode {
	/**
	 * Clear all color buffers.
	 */
	COLOR,
	/**
	 * Clear the depth buffer.
	 */
	DEPTH,
	/**
	 * Clear the stencil buffer.
	 */
	STENCIL
}
